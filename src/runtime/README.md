# Runtime

Build-Time Runtime Projection and Per-Game Runtime Model.

This directory will contain:

- Build-time projection logic (Editorial Source → Runtime Model)
- Runtime Game Model types
- Per-game optimized data structures
- Canonical game route generation

The Runtime Model is computed at build time from the Editorial Source Model and embedded in the static build.

Not yet implemented in Repository Bootstrap v0.1.
